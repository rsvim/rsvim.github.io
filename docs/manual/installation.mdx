---
sidebar_position: 2
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Installation

## Download

You can download pre-built binary from GitHub release page:

<Tabs>
  <TabItem value="linux" label="linux" default>
    ```bash
    curl -fsSL https://github.com/rsvim/rsvim/releases/latest/download/rsvim-x86_64-unknown-linux-gnu.tar.gz -o rsvim.tar.gz
    ```
  </TabItem>
  <TabItem value="mac" label="mac">
    ```bash
    curl -fsSL https://github.com/rsvim/rsvim/releases/latest/download/rsvim-aarch64-apple-darwin.tar.gz -o rsvim.tar.gz
    ```
  </TabItem>
  <TabItem value="windows" label="windows">
    ```powershell
    curl -fsSL https://github.com/rsvim/rsvim/releases/latest/download/rsvim-x86_64-pc-windows-msvc.tar.gz -o rsvim.tar.gz
    ```
  </TabItem>
</Tabs>

For nightly pre-builts, download the `nightly` tag pre-release:


<Tabs>
  <TabItem value="linux" label="linux" default>
    ```bash
    curl -fsSL https://github.com/rsvim/rsvim/releases/tag/nightly/download/rsvim-x86_64-unknown-linux-gnu.tar.gz -o rsvim.tar.gz
    ```
  </TabItem>
  <TabItem value="mac" label="mac">
    ```bash
    curl -fsSL https://github.com/rsvim/rsvim/releases/tag/nightly/download/rsvim-aarch64-apple-darwin.tar.gz -o rsvim.tar.gz
    ```
  </TabItem>
  <TabItem value="windows" label="windows">
    ```powershell
    curl -fsSL https://github.com/rsvim/rsvim/releases/tag/nightly/download/rsvim-x86_64-pc-windows-msvc.tar.gz -o rsvim.tar.gz
    ```
  </TabItem>
</Tabs>

## Build From Source

Build with `cargo`:

```bash
cargo install --locked rsvim
```

For the latest updates, build with `main` branch:

```bash
cargo install --locked rsvim --git https://github.com/rsvim/rsvim.git --branch main
```
